---
name: dependency-upgrade
description: 安全升级软件依赖、框架或运行时。用于 npm/pnpm/yarn、composer、pip/uv、Maven/Gradle、Go modules、framework upgrade、security patch 和 breaking-change migration。
---
# 依赖升级
执行仓库根目录 `AI_SKILLS.md` 的 **16. Dependency Upgrade｜依赖升级** 章节，并遵守 `AGENTS.md`。按逻辑批次升级并验证 clean install、build 与测试。
