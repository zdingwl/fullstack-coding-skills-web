---
name: default-branch-delivery
description: 完成代码任务后的默认 Git 交付技能。用于 commit、push、final delivery；验证通过后确定 origin 真实默认分支，安全同步远端并直接 push 默认分支，不默认创建 PR，不 force push。
---
# 默认分支直接交付
执行仓库根目录 `AI_SKILLS.md` 的 **22. Default Branch Delivery｜默认分支直接交付** 章节，并严格遵守 `AGENTS.md`。这是每个代码修改任务的最终强制步骤。
