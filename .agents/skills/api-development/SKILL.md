---
name: api-development
description: 设计、实现和演进 HTTP/REST/GraphQL/RPC API。用于 endpoint、request/response schema、pagination、error model、versioning、idempotency、webhook 和 third-party API integration。
---
# API 开发
执行仓库根目录 `AI_SKILLS.md` 的 **8. API Development｜接口开发** 章节，并遵守 `AGENTS.md`。修改已有契约前必须检查调用方和兼容性。
