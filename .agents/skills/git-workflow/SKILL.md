---
name: git-workflow
description: 管理安全的 Git 变更、提交、同步和冲突。用于 git status/diff、commit、rebase、conflict resolution、change isolation 和提交前检查。
---
# Git 工作流
执行仓库根目录 `AI_SKILLS.md` 的 **17. Git Workflow｜Git 安全工作流** 章节，并遵守 `AGENTS.md`。本仓库默认不建 feature branch/PR，最终交付同时必须使用 default-branch-delivery。
